
= form_tag [:refund, @order, @payment], :class => 'refundForm' do
  %h2= t('vinus.refund.issue_refund')
  %p.intro
    = t('vinus.refund.intro_html', amount: number_to_currency(@payment.refundable_amount))

  .moneyInput
    .currency= Vinus.settings.currency_unit.html_safe
    = text_field_tag 'amount', @payment.refundable_amount, :class => 'text'
  %p.submit
    = submit_tag t('vinus.refund.refund'), :class => 'button green'
    = link_to t('vinus.close'), '#', :class => 'button grey', :rel => 'closeDialog'

