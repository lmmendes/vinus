- @page_title = t('vinus.users.users')

= content_for :header do
  %p.buttons= link_to t('vinus.users.new_user'), :new_user, :class => 'button green'
  %h2.users= t('vinus.users.users')

.table
  %table.data
    %thead
      %tr
        %th= t('vinus.users.name')
        %th= t('vinus.users.email')
    %tbody
      - for user in @users
        %tr
          %td= link_to user.full_name, [:edit, user]
          %td= user.email_address
