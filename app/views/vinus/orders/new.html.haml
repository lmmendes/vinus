- @page_title = t('vinus.orders.new_order')

= content_for :header do
  %p.buttons= link_to t('vinus.orders.back_to_orders'), :orders, :class => 'button grey'
  %h2.orders= t('vinus.orders.new_order')

= form_for @order, :html => {:class => 'orderForm newOrder'} do |f|
  = f.error_messages
  = render 'form', :f => f
  = field_set_tag t('vinus.orders.ordered_products'), :class => 'padded' do
    = render 'order_items_form', :f => f
  %p.submit
    = f.submit t('vinus.orders.create_order'), :class => 'button green'
    = link_to t('vinus.cancel'), :orders, :class => 'button grey'
