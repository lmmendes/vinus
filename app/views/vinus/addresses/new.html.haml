- @page_title = "New Address for #{@customer.name}"
= content_for :header do
  %p.buttons= link_to t('vinus.addresses.back_to_customer'), @customer, class: 'button'
  %h2.users= t('vinus.addresses.addresses')
= render 'form'
