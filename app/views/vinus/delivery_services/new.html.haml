- @page_title = t('vinus.delivery_services.delivery_services')

= content_for :header do
  %p.buttons= link_to t('vinus.delivery_services.back'), :delivery_services, class: 'button'
  %h2.delivery_services= t('vinus.delivery_services.delivery_services')
= render 'form'
