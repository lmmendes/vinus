- @page_title = t('vinus.products.products')

= content_for :header do
  %p.buttons
    = link_to t('vinus.products.new_product'), :new_product, :class => 'button green'
    = link_to t('vinus.products.import_products'), :import_products, :class => 'button gray'
  %h2.products= t('vinus.products.products')

= render 'table', :products => @products

= paginate @products_paged
